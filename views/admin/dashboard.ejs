<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard Admin - Quiz Jeux Vidéo</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="admin-layout">
    <%- include('partials/nav') %>

    <div class="admin-content">
      <div class="page-header">
        <h1>📊 Dashboard</h1>
      </div>

      <div class="stats-grid">
        <div class="stat-card">
          <div class="stat-icon">👥</div>
          <div class="stat-info">
            <div class="stat-value"><%= stats.total_sessions || 0 %></div>
            <div class="stat-label">Sessions totales</div>
          </div>
        </div>

        <div class="stat-card">
          <div class="stat-icon">✅</div>
          <div class="stat-info">
            <div class="stat-value"><%= stats.completed_sessions || 0 %></div>
            <div class="stat-label">Sessions complétées</div>
          </div>
        </div>

        <div class="stat-card">
          <div class="stat-icon">❓</div>
          <div class="stat-info">
            <div class="stat-value"><%= questionCount %></div>
            <div class="stat-label">Questions</div>
          </div>
        </div>

        <div class="stat-card">
          <div class="stat-icon">📈</div>
          <div class="stat-info">
            <div class="stat-value"><%= stats.avg_score ? Math.round(stats.avg_score) : 0 %></div>
            <div class="stat-label">Score moyen</div>
          </div>
        </div>
      </div>

      <div class="dashboard-sections">
        <div class="section">
          <h2>📂 Catégories</h2>
          <div class="categories-list">
            <% categories.forEach(cat => { %>
              <div class="category-chip">
                <%= cat.category %> (<%= cat.count %>)
              </div>
            <% }); %>
          </div>
        </div>

        <div class="section">
          <h2>🎮 Sessions récentes</h2>
          <div class="sessions-table">
            <table>
              <thead>
                <tr>
                  <th>Joueur</th>
                  <th>Score</th>
                  <th>Date</th>
                  <th>Statut</th>
                </tr>
              </thead>
              <tbody>
                <% recentSessions.forEach(session => { %>
                  <tr>
                    <td><%= session.player_name %></td>
                    <td><strong><%= session.score %></strong></td>
                    <td><%= new Date(session.started_at).toLocaleString('fr-FR') %></td>
                    <td>
                      <% if (session.completed_at) { %>
                        <span class="badge badge-success">Terminée</span>
                      <% } else { %>
                        <span class="badge badge-warning">En cours</span>
                      <% } %>
                    </td>
                  </tr>
                <% }); %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
