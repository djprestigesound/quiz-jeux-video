<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>🏆 Classement en Direct - Quiz Jeux Vidéo</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="display-mode" style="margin: 0; padding: 0; background: var(--bg-primary); overflow: hidden;">
  <!-- Header compact -->
  <div style="text-align: center; padding: 10px 20px; border-bottom: 2px solid var(--primary);">
    <div style="display: flex; align-items: center; justify-content: center; gap: 15px; margin-bottom: 5px;">
      <img src="/images/branding/logo.png" alt="VERDON" style="height: 40px;">
      <span style="color: var(--text-secondary); font-size: 1.2rem;">&</span>
      <img src="/images/branding/dj_logo.jpg" alt="DJ Prestige Sound" style="height: 40px;">
    </div>
    <h1 style="font-size: 2rem; margin: 0;">🏆 Classement en Direct</h1>
  </div>

  <!-- 3 colonnes pour les 3 quiz -->
  <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 15px; height: calc(100vh - 120px); padding: 15px; overflow: hidden;">

    <!-- Quiz 1: Classiques -->
    <div style="display: flex; flex-direction: column; overflow: hidden; background: linear-gradient(135deg, rgba(255, 0, 110, 0.1) 0%, rgba(255, 0, 110, 0.05) 100%); border-radius: 15px; border: 2px solid rgba(255, 0, 110, 0.3); padding: 15px;">
      <h2 style="font-size: 1.8rem; margin: 0 0 15px 0; text-align: center; color: var(--primary);">🕹️ Classiques</h2>

      <div style="flex: 1; overflow-y: auto; padding-right: 5px;">
        <% if (leaderboardQuiz1.length === 0) { %>
          <div style="text-align: center; padding: 50px 10px; color: var(--text-secondary);">
            <p style="font-size: 3rem; margin-bottom: 10px;">🎮</p>
            <p style="font-size: 1rem;">Aucun joueur</p>
          </div>
        <% } else { %>
          <% leaderboardQuiz1.forEach((entry, index) => { %>
            <div style="background: var(--bg-card); margin-bottom: 8px; padding: 10px 12px; border-radius: 8px; display: flex; align-items: center; gap: 10px; border: 1px solid <%= index < 3 ? 'var(--primary)' : 'var(--border)' %>;">
              <!-- Rang -->
              <div style="font-size: 1.3rem; font-weight: bold; min-width: 40px; text-align: center;">
                <% if (index === 0) { %>
                  🥇
                <% } else if (index === 1) { %>
                  🥈
                <% } else if (index === 2) { %>
                  🥉
                <% } else { %>
                  <span style="color: var(--text-secondary); font-size: 1rem;">#<%= index + 1 %></span>
                <% } %>
              </div>
              <!-- Nom -->
              <div style="flex: 1; min-width: 0;">
                <div style="font-size: 1.2rem; font-weight: bold; color: var(--text-primary); white-space: nowrap; overflow: hidden; text-overflow: ellipsis;"><%= entry.player_name %></div>
                <div style="font-size: 0.8rem; color: var(--text-secondary);"><%= entry.correct_answers %>/<%= entry.total_questions %></div>
              </div>
              <!-- Score -->
              <div style="font-size: 1.5rem; font-weight: bold; color: var(--primary); min-width: 60px; text-align: right;">
                <%= entry.score %>
              </div>
            </div>
          <% }); %>
        <% } %>
      </div>
    </div>

    <!-- Quiz 2: Modernes -->
    <div style="display: flex; flex-direction: column; overflow: hidden; background: linear-gradient(135deg, rgba(99, 102, 241, 0.1) 0%, rgba(99, 102, 241, 0.05) 100%); border-radius: 15px; border: 2px solid rgba(99, 102, 241, 0.3); padding: 15px;">
      <h2 style="font-size: 1.8rem; margin: 0 0 15px 0; text-align: center; color: #6366F1;">🎮 Modernes</h2>

      <div style="flex: 1; overflow-y: auto; padding-right: 5px;">
        <% if (leaderboardQuiz2.length === 0) { %>
          <div style="text-align: center; padding: 50px 10px; color: var(--text-secondary);">
            <p style="font-size: 3rem; margin-bottom: 10px;">🎮</p>
            <p style="font-size: 1rem;">Aucun joueur</p>
          </div>
        <% } else { %>
          <% leaderboardQuiz2.forEach((entry, index) => { %>
            <div style="background: var(--bg-card); margin-bottom: 8px; padding: 10px 12px; border-radius: 8px; display: flex; align-items: center; gap: 10px; border: 1px solid <%= index < 3 ? '#6366F1' : 'var(--border)' %>;">
              <!-- Rang -->
              <div style="font-size: 1.3rem; font-weight: bold; min-width: 40px; text-align: center;">
                <% if (index === 0) { %>
                  🥇
                <% } else if (index === 1) { %>
                  🥈
                <% } else if (index === 2) { %>
                  🥉
                <% } else { %>
                  <span style="color: var(--text-secondary); font-size: 1rem;">#<%= index + 1 %></span>
                <% } %>
              </div>
              <!-- Nom -->
              <div style="flex: 1; min-width: 0;">
                <div style="font-size: 1.2rem; font-weight: bold; color: var(--text-primary); white-space: nowrap; overflow: hidden; text-overflow: ellipsis;"><%= entry.player_name %></div>
                <div style="font-size: 0.8rem; color: var(--text-secondary);"><%= entry.correct_answers %>/<%= entry.total_questions %></div>
              </div>
              <!-- Score -->
              <div style="font-size: 1.5rem; font-weight: bold; color: #6366F1; min-width: 60px; text-align: right;">
                <%= entry.score %>
              </div>
            </div>
          <% }); %>
        <% } %>
      </div>
    </div>

    <!-- Quiz 3: Culture Gaming -->
    <div style="display: flex; flex-direction: column; overflow: hidden; background: linear-gradient(135deg, rgba(251, 191, 36, 0.1) 0%, rgba(251, 191, 36, 0.05) 100%); border-radius: 15px; border: 2px solid rgba(251, 191, 36, 0.3); padding: 15px;">
      <h2 style="font-size: 1.8rem; margin: 0 0 15px 0; text-align: center; color: #FBBF24;">🏆 Culture</h2>

      <div style="flex: 1; overflow-y: auto; padding-right: 5px;">
        <% if (leaderboardQuiz3.length === 0) { %>
          <div style="text-align: center; padding: 50px 10px; color: var(--text-secondary);">
            <p style="font-size: 3rem; margin-bottom: 10px;">🎮</p>
            <p style="font-size: 1rem;">Aucun joueur</p>
          </div>
        <% } else { %>
          <% leaderboardQuiz3.forEach((entry, index) => { %>
            <div style="background: var(--bg-card); margin-bottom: 8px; padding: 10px 12px; border-radius: 8px; display: flex; align-items: center; gap: 10px; border: 1px solid <%= index < 3 ? '#FBBF24' : 'var(--border)' %>;">
              <!-- Rang -->
              <div style="font-size: 1.3rem; font-weight: bold; min-width: 40px; text-align: center;">
                <% if (index === 0) { %>
                  🥇
                <% } else if (index === 1) { %>
                  🥈
                <% } else if (index === 2) { %>
                  🥉
                <% } else { %>
                  <span style="color: var(--text-secondary); font-size: 1rem;">#<%= index + 1 %></span>
                <% } %>
              </div>
              <!-- Nom -->
              <div style="flex: 1; min-width: 0;">
                <div style="font-size: 1.2rem; font-weight: bold; color: var(--text-primary); white-space: nowrap; overflow: hidden; text-overflow: ellipsis;"><%= entry.player_name %></div>
                <div style="font-size: 0.8rem; color: var(--text-secondary);"><%= entry.correct_answers %>/<%= entry.total_questions %></div>
              </div>
              <!-- Score -->
              <div style="font-size: 1.5rem; font-weight: bold; color: #FBBF24; min-width: 60px; text-align: right;">
                <%= entry.score %>
              </div>
            </div>
          <% }); %>
        <% } %>
      </div>
    </div>

  </div>

  <!-- Footer info -->
  <div style="text-align: center; padding: 8px; background: rgba(255, 0, 110, 0.1); border-top: 2px solid var(--primary);">
    <div style="display: flex; align-items: center; justify-content: center; gap: 20px;">
      <span style="color: var(--text-secondary); font-size: 0.9rem;">
        <span class="refresh-dot" style="display: inline-block; width: 8px; height: 8px; background: var(--primary); border-radius: 50%; margin-right: 6px; animation: pulse 1s ease-in-out infinite;"></span>
        Mise à jour toutes les 3 secondes
      </span>
      <% if (activePlayers && activePlayers.length > 0) { %>
        <span style="color: var(--primary); font-size: 0.9rem; font-weight: bold;">
          🎮 <%= activePlayers.length %> joueur<%= activePlayers.length > 1 ? 's' : '' %> en cours
        </span>
      <% } %>
    </div>
  </div>

  <style>
    @keyframes pulse {
      0%, 100% {
        opacity: 0.6;
        transform: scale(1);
      }
      50% {
        opacity: 1;
        transform: scale(1.2);
      }
    }

    /* Scrollbar styling */
    ::-webkit-scrollbar {
      width: 6px;
    }

    ::-webkit-scrollbar-track {
      background: rgba(255, 255, 255, 0.05);
      border-radius: 3px;
    }

    ::-webkit-scrollbar-thumb {
      background: var(--primary);
      border-radius: 3px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: rgba(255, 0, 110, 0.8);
    }
  </style>

  <script>
    // Auto-refresh toutes les 3 secondes
    let refreshTimer;

    function startAutoRefresh() {
      refreshTimer = setInterval(() => {
        window.location.reload();
      }, 3000);
    }

    // Animation du point de refresh
    setInterval(() => {
      const dot = document.querySelector('.refresh-dot');
      if (dot) {
        dot.style.animation = 'none';
        setTimeout(() => {
          dot.style.animation = 'pulse 1s ease-in-out';
        }, 10);
      }
    }, 3000);

    // Démarrer l'auto-refresh
    startAutoRefresh();

    // Nettoyer l'intervalle
    window.addEventListener('beforeunload', () => {
      if (refreshTimer) {
        clearInterval(refreshTimer);
      }
    });

    // Empêcher le scroll accidentel
    document.body.style.overflow = 'hidden';

    // Désactiver le zoom sur double-tap (pour écran tactile)
    document.addEventListener('dblclick', (e) => {
      e.preventDefault();
    });
  </script>
</body>
</html>
