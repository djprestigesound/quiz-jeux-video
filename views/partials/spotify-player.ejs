<!-- Spotify Player - 40 Classic Video Game Songs -->
<div class="spotify-player" id="spotifyPlayer">
  <button class="spotify-toggle" id="spotifyToggle" title="Musique d'ambiance gaming">
    🎵
  </button>

  <div class="spotify-content" id="spotifyContent">
    <div class="spotify-header">
      <span class="spotify-title">🎮 Ambiance Gaming</span>
      <button class="spotify-close" id="spotifyClose">✕</button>
    </div>
    <iframe
      style="border-radius:12px"
      src="https://open.spotify.com/embed/album/2UGmdtMnIlUMiXryHZ8StP?utm_source=generator&theme=0"
      width="100%"
      height="352"
      frameBorder="0"
      allowfullscreen=""
      allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture"
      loading="lazy">
    </iframe>
  </div>
</div>

<script>
  // Gestion du lecteur Spotify
  const spotifyPlayer = document.getElementById('spotifyPlayer');
  const spotifyToggle = document.getElementById('spotifyToggle');
  const spotifyClose = document.getElementById('spotifyClose');
  const spotifyContent = document.getElementById('spotifyContent');

  // Ouvrir le lecteur
  spotifyToggle.addEventListener('click', () => {
    spotifyPlayer.classList.add('open');
    localStorage.setItem('spotifyPlayerOpen', 'true');
  });

  // Fermer le lecteur
  spotifyClose.addEventListener('click', () => {
    spotifyPlayer.classList.remove('open');
    localStorage.setItem('spotifyPlayerOpen', 'false');
  });

  // Restaurer l'état du lecteur
  if (localStorage.getItem('spotifyPlayerOpen') === 'true') {
    spotifyPlayer.classList.add('open');
  }
</script>
